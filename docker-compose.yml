services:
  frontend:
    build:
      context: ./intelligent-front
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: "https://beerbears.website"
    container_name: tender-hack-frontend
    ports:
      - "90:90"
    restart: unless-stoppedfir